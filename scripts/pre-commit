#!/bin/sh
# Git pre-commit hook — blocks a commit if scripts/check_no_secrets.py finds
# anything key-shaped in the staged files. Not installed automatically
# (git doesn't track .git/hooks), see README "Security" section for the
# one-line install command.
python scripts/check_no_secrets.py
exit $?
